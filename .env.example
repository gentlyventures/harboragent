##############################################
# Harbor Agent – Environment Variables (Example)
# Copy this file to `.env` and fill in values.
# DO NOT COMMIT `.env` TO GIT.
##############################################

############################
# Stripe Credentials
############################

# Stripe Secret Key (test or live)
# Format: sk_test_xxx or sk_live_xxx
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# (Optional) Stripe Price ID to enforce product identity
# Format: price_12345abc
GENESIS_PACK_PRICE_ID=price_your_price_id_here

############################
# Cloudflare Worker / Wrangler
############################

# Cloudflare account ID
# Found under: Cloudflare Dashboard → Workers → Overview
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here

# Cloudflare API Token with:
# - Workers Scripts:Edit
# - Workers Scripts:Read
# - Account:Read
# DO NOT USE YOUR GLOBAL API KEY.
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

############################
# Cloudflare Worker Runtime Variables
# (set via wrangler secret put)
############################

# URL where the Genesis Pack files are hosted
# This should be a CDN or storage bucket URL
# Example: https://your-cdn-or-storage.example.com/genesis-pack
DOWNLOAD_ORIGIN_URL=https://your-cdn-or-storage.example.com/genesis-pack

############################
# Local Development Overrides (Optional)
############################

# For local testing without Cloudflare Worker
LOCAL_STRIPE_SECRET_KEY=sk_test_your_local_stripe_key
LOCAL_DOWNLOAD_ORIGIN_URL=http://localhost:8000/genesis-pack

############################
# General Notes
############################

# 1. After copying this to `.env`, fill in the values locally.
# 2. Ensure `.env` is in your `.gitignore`.
# 3. GitHub Actions will not use `.env`; you must add secrets
#    manually using the GitHub Secrets UI.
# 4. Cloudflare Workers will not use `.env`; you must set secrets
#    using `wrangler secret put`.
# 5. This is ONLY an example schema — replace placeholder values.
